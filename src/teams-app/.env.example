# Microsoft Bot registration
BOT_ID=00000000-0000-0000-0000-000000000000
BOT_PASSWORD=

# Backend RAG service endpoint (base URL without trailing slash)
RAG_API_BASE_URL=http://localhost:5000

# Comma-separated origins allowed for CORS / iframe embedding
ALLOWED_ORIGINS=https://localhost:5300,http://localhost:5173,http://localhost:3978

# Optional tab base URL override (used for deep links and Action.OpenUrl)
TAB_BASE_URL=http://localhost:5300
# Optional Teams app ID to build entity deep links for the tab
TEAMS_APP_ID=

# Express server port
PORT=3978

# Optional proxy configuration
HTTP_PROXY=
HTTPS_PROXY=
REQUEST_TIMEOUT_MS=20000
REQUEST_RETRY_COUNT=2
